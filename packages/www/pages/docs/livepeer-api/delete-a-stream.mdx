import DocsLayout from "../../../components/DocsLayout";

<DocsLayout>

### Delete a Stream

You can delete a stream by sending the `DELETE` request with a specific "streamId".  For example:

```bash
curl -H 'authorization: Bearer {api_key}' \
-X DELETE "https://livepeer.com/api/stream/{streamId}"
```

</DocsLayout>