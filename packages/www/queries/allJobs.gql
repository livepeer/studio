query allJob($where: JobFilter) {
  allJob(where: $where) {
    _id
    title
    slug {
      current
    }
    body
    metaTitle
    metaDescription
    metaUrl
    openGraphImage {
      asset {
        altText
        url
      }
    }
  }
}
